# weight-noise-quantization

Quick setup for weight noise and quantization experiments.

## Setup

```bash
python -m venv .venv
.venv\Scripts\activate   # Windows
pip install -r requirements.txt
```

## Usage

TBD.
